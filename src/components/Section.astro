---
interface Props {
  label?: string;
  title?: string;
  subtitle?: string;
  background?: "white" | "cream" | "ink";
  centered?: boolean;
  narrow?: boolean;
  class?: string;
}

const {
  label,
  title,
  subtitle,
  background = "white",
  centered = false,
  narrow = false,
  class: extraClass = "",
} = Astro.props;

const bgMap = {
  white: "bg-surface",
  cream: "bg-ground",
  ink:   "bg-ground",
};

const textMap = {
  white: "text-lav",
  cream: "text-lav",
  ink:   "text-lav",
};

const subtitleMap = {
  white: "text-lav-300",
  cream: "text-lav-300",
  ink:   "text-lav-300",
};

const labelMap = {
  white: "text-violet-400",
  cream: "text-violet-400",
  ink:   "text-violet-300",
};
---

<section class:list={[bgMap[background], "section-pad", extraClass]}>
  <div class:list={[narrow ? "container-narrow" : "container-site"]}>
    {(label || title || subtitle) && (
      <div class:list={["mb-12 fade-up", centered && "text-center"]}>
        {label && (
          <p class:list={["label-caps mb-3", labelMap[background]]}>
            {label}
          </p>
        )}
        {title && (
          <h2 class:list={["font-display text-4xl font-semibold lg:text-5xl", textMap[background]]}>
            {title}
          </h2>
        )}
        {subtitle && (
          <p class:list={["mt-4 max-w-prose text-base font-body leading-relaxed", subtitleMap[background], centered && "mx-auto"]}>
            {subtitle}
          </p>
        )}
        <div class:list={["mt-5 h-px w-12 bg-gradient-to-r from-violet-400 to-transparent", centered && "mx-auto"]} />
      </div>
    )}
    <slot />
  </div>
</section>
