---
import { getCollection } from "astro:content";
import BaseLayout from "../../layouts/BaseLayout.astro";
import Button from "../../components/Button.astro";

export async function getStaticPaths() {
  const news = await getCollection("news");
  return news.map((item) => ({
    params: { slug: item.id },
    props: { item },
  }));
}

const { item } = Astro.props;
const { Content } = await item.render();

const months = ["January","February","March","April","May","June","July","August","September","October","November","December"];

const typeLabel: Record<string, string> = {
  "meeting-notes": "Meeting Notes",
  "announcement":  "Announcement",
  "update":        "Update",
};
---

<BaseLayout title={`${item.data.title} — GADI`} description={item.data.description}>
  <!-- Header -->
  <section class="border-b border-line bg-ground">
    <div class="container-site py-16">
      <div class="mb-4 flex flex-wrap items-center gap-3">
        <a href="/news" class="text-xs font-body text-lav-400 hover:text-violet-300 transition-colors">
          ← All News
        </a>
        <span class="text-lav-300">|</span>
        <span class="label-caps text-violet-400">{typeLabel[item.data.type]}</span>
      </div>
      <h1 class="font-display text-4xl font-bold text-lav lg:text-5xl max-w-3xl">
        {item.data.title}
      </h1>
      <div class="mt-5 h-px w-16 bg-gradient-to-r from-violet-400 to-transparent" />
      <div class="mt-5 flex flex-wrap items-center gap-6 text-sm font-body text-lav-400">
        <span>
          {item.data.date.getDate()} {months[item.data.date.getMonth()]} {item.data.date.getFullYear()}
        </span>
        {item.data.author && <span>{item.data.author}</span>}
      </div>
    </div>
  </section>

  <!-- Content -->
  <section class="bg-surface">
    <div class="container-narrow py-16">
      <div class="prose prose-stone max-w-none font-body
        prose-headings:font-display prose-headings:font-semibold prose-headings:text-lav
        prose-p:text-lav-200 prose-p:leading-relaxed
        prose-a:text-violet-400 prose-a:no-underline hover:prose-a:underline
        prose-strong:text-lav
        prose-ul:text-lav-200
        prose-li:marker:text-violet-400
        prose-h2:text-3xl prose-h3:text-xl">
        <Content />
      </div>
    </div>
  </section>

  <div class="border-t border-line bg-surface">
    <div class="container-site py-6">
      <Button href="/news" variant="ghost" size="sm">← Back to News</Button>
    </div>
  </div>
</BaseLayout>
